# whatsapp-group-admin-bot-ar

بوت إدارة مجموعات واتساب (غير رسمي) يعمل عبر WhatsApp Web باستخدام Baileys.

## تشغيل سريع
- يتطلب Node.js >= 20
- `npm install`
- `npm start`

## تسجيل الدخول (QR)
- عند أول تشغيل سيظهر QR في الطرفية.
- امسح QR من واتساب > الأجهزة المرتبطة.
- إذا ظهرت في السجلات رسالة تفيد بتسجيل الخروج، يلزم إعادة ربط الجلسة (أعد تشغيل البوت بعد تنظيف مجلد المصادقة إذا كان ذلك مطلوبًا).

## الإعدادات (ENV)
- `BOT_PREFIX` (افتراضي: `!`)
- `BOT_AUTH_DIR` (افتراضي: `./data/auth`)
- `BOT_STORAGE_PATH` (افتراضي: `./data/store.json`)
- `BOT_LOG_LEVEL` (افتراضي: `info`)
- `BAILEYS_LOG_LEVEL` (افتراضي: `warn`)
- `BOT_PING_RESPONSE` (افتراضي: `🏓 بونج! البوت يعمل ✅`)
- `BOT_ALLOWLIST` (قائمة أرقام/معرفات مخولين مفصولة بمسافة أو فاصلة)
- `BOT_REQUIRE_CALLER_ADMIN` (افتراضي: `false`)
- `BOT_MOD_WARN_COOLDOWN_MS` (افتراضي: `15000`)
- `BOT_CMD_COOLDOWN_MS` (افتراضي: `1200`)
- `BOT_FUN_COOLDOWN_MS` (افتراضي: `6000`)

## الصلاحيات
- أوامر الإدارة والإشراف المحمية تعمل فقط للمخولين عبر `BOT_ALLOWLIST`.
- عند عدم ضبط قائمة السماح، ستُرفض الأوامر المحمية للجميع.

## ملاحظات مهمة
- هذا حل غير رسمي وقد يتوقف عن العمل أو يتسبب في قيود/حظر للحساب بسبب تغييرات أو سياسات واتساب.
- واجهة القوائم التفاعلية تعمل بأفضل جهد، ويوجد بديل نصي دائمًا.
